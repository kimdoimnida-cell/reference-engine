# Reference Engine — 레퍼런스 → 김대영(YLZ) 콘텐츠 재창작

레퍼런스 링크(인스타 릴스/유튜브 숏츠/틱톡)를 받아 **캡션·대사·메타 추출 → (해외면)한국어 번역 → 해부 → 김대영/YLZ 식 재창작**까지 하는 파이프라인. `/레퍼런스` 스킬로 구동.

## 핵심 자산
- `YLZ_Style_Bible.md` — 재창작 규칙서. **재창작 단계에서 전체를 시스템 프롬프트로 로드.** 김대영/김디오 두 모드 분리.
- `tools/stt.py` — STT 폴백(yt-dlp + faster-whisper). 유튜브 쌍둥이를 못 찾은 릴스의 대사 확보용.
- `output/<날짜_슬러그>/` — 잡별 산출물(원본추출·번역·해부·재창작).

## 대사(스크립트) 확보 2-트랙
1. **1순위 — 유튜브 쌍둥이**: 크리에이터명 + 캡션 키워드로 YouTubeData MCP 검색 → 같은 콘텐츠 영상의 transcript 추출. 무료·빠름·기본값.
2. **폴백 — 로컬 STT**: 못 찾으면 `python tools/stt.py "<URL>"`. `faster-whisper` 설치됨, `yt-dlp`는 최초 1회 `pip install yt-dlp` 필요.

## 추출 현실 (플랫폼별)
- **유튜브/숏츠**: YouTubeData MCP로 details+transcript+comments 완전 자동.
- **인스타 릴스**: 라이브 Graph API 토큰 없음 → WebFetch로 캡션·작성자 시도 → 실패 시 사용자에게 캡션/핸들 요청 → 유튜브 쌍둥이로 대사 확보.
- **틱톡**: 스크래핑 불안정 → 유튜브 쌍둥이 또는 STT 폴백.

## 규칙
- 산출물은 채팅에 바로 출력(경로만 X). 파일은 output에도 저장.
- 재창작은 항상 Style Bible 모드 스위치 먼저 판단(기본 김대영). 비즈=김대영, 상황극/글로벌엔터=김디오. 절대 섞지 않음.
- 임의 HEX 색상코드·기계적 고정 해시태그 넣지 않기(Bible §8·§9).
